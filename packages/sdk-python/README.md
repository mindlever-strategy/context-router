# Experimental Python SDK

This directory is an unpublished prototype. Its connection lifecycle and public
contract are not complete, and it is not part of the `v0.1.0` release.

Use the MCP tool API directly or `@context-router/sdk`. Python package work is
tracked for a possible `v0.2`.
